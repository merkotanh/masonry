%h1= @image.title
%p= @image.description
/ # = image_tag @image.ipath.url(:thumb)

= link_to 'Back', root_path
= link_to 'Edit', edit_image_path
= link_to 'Delete', image_path, method: :delete, data: { confirm: 'Are u sure?' }