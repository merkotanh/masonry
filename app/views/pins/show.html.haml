%h1= @image.title
%p= @image.description
= image_tag @image.image.url

= link_to 'Back', root_path
= link_to 'Edit', edit_pin_path
= link_to 'Delete', pin_path, method: :delete, data: { confirm: 'Are u sure?' }